<!DOCTYPE html>
<html>
<head>
  <title>HTML 검색 기능 예시</title>
  <style>
    #search {
      background-image: url('/css/searchicon.png'); /* 검색 아이콘 추가 */
      background-position: 10px 12px; /* 아이콘 위치 조정 */
      background-repeat: no-repeat; /* 아이콘 반복 방지 */
      width: 100%; /* 전체 너비 */
      font-size: 16px; /* 폰트 크기 */
      padding: 12px 20px 12px 40px; /* 여백 */
      border: 1px solid #ddd; /* 회색 테두리 */
      margin-bottom: 12px; /* 아래쪽 공간 */
    }

    #result {
      /* 기본 리스트 스타일 제거 */
      list-style-type: none;
      padding: 0;
      margin: 0;
    }

    #result li a {
      border: 1px solid #ddd; /* 모든 링크에 테두리 추가 */
      margin-top: -1px; /* 이중 테두리 방지 */
      background-color: #f6f6f6; /* 회색 배경 색상 */
      padding: 12px; /* 여백 */
      text-decoration: none; /* 밑줄 제거 */
      font-size: 18px; /* 폰트 크기 */
      color: black; /* 검은색 텍스트 색상 */
      display: block; /* 블록 요소로 만들기 */
    }

    #result li a:hover:not(.header) {
      background-color: #eee; /* 헤더가 아닌 링크에 마우스 오버 효과 추가 */
    }
  </style>
</head>
<body>
  <h1>HTML 검색 기능 예시</h1>
  <p>검색창에 원하는 단어를 입력하면 관련된 항목이 나타납니다.</p>
  <input type="text" id="search" onkeyup="searchFunction()" placeholder="검색어를 입력하세요">
  <ul id="result">
    <li><a href="#">HTML</a></li>
    <li><a href="#">CSS</a></li>
    <li><a href="#">JavaScript</a></li>
    <li><a href="#">Python</a></li>
    <li><a href="#">Java</a></li>
    <li><a href="#">PHP</a></li>
  </ul>
  <script>
    function searchFunction() {
      // 변수 선언
      var input, filter, ul, li, a, i, txtValue;
      input = document.getElementById('search');
      filter = input.value.toUpperCase();
      ul = document.getElementById("result");
      li = ul.getElementsByTagName('li');
      // 모든 리스트 항목을 순회하면서 검색어와 일치하는 것이 있는지 확인
      for (i = 0; i < li.length; i++) {
        a = li[i].getElementsByTagName("a")[0];
        txtValue = a.textContent || a.innerText;
        if (txtValue.toUpperCase().indexOf(filter) > -1) {
          li[i].style.display = "";
        } else {
          li[i].style.display = "none";
        }
      }
    }
  </script>
</body>
</html>
